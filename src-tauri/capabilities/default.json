{
  "identifier": "default",
  "description": "Default capability for chatlog_alpha",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-execute",
    "shell:allow-stdin-write",
    "dialog:default"
  ]
}
